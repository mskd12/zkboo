Original authors: Liang et al.
OpenSSL v1.1.0 is added by Fan Zhang.
SHA2, HMAC is added by Deepak Maram.

---- original README ----

sudo apt-get install libssl-dev
run in root
if run into stack overflow, use "ulimit -s unlimited" to increase stack size
